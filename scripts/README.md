# Scripts

Place your own PowerShell scripts here.

Copy a template from `../templates/` to get started:

```powershell
Copy-Item ../templates/basic-script.ps1 ./my-script.ps1
```

Scripts in this directory are yours – they are not committed as part of the
starter kit (add a `.gitignore` entry if you want to keep them private).
